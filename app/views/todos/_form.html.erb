<% if @todo.errors.any? %>
    <h2>Se encontraron los siguientes errores</h2>
    <ul>
        <% @todo.errors.full_messages.each do |msg| %>
        <li> <%=  msg %>  </li>      
        
        <% end%>

    </ul>
<% end %>
<%= form_for @todo do |f|  %>
    <p>
        <%= f.label  "Referencia"%> <br>
        <%= f.text_field :name %>
    </p>
    <p>
        <%= f.label "Descripción" %> <br>
        <%= f.text_area :description %>
    </p>

<p>
<%= f.submit "Enviar" %>
</p>


<% end %>
<%= link_to "Ir al inicio", todos_path(@todo) %>